{
  "name": "root",
  "private": false,
  "version":"0.0.1",
  "devDependencies": {
    "lerna": "~3.22.1"
  },
  "scripts": {
	"install": "lerna run build",
    "lerna": "lerna",
    "build": "lerna run build",
    "pack": "lerna run pack",
    "prettier": "lerna run prettier",
    "test": "lerna run test"
  }
}
